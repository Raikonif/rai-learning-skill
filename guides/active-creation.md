# Active Creation Guide

## When `active_creation_path = yes`
- create minimal scoped artifacts tied to current subtopic,
- install dependencies only when needed and permitted,
- add tests/validation when useful,
- explain what was created and why.

## When `active_creation_path = no`
- keep implementation examples inline in conversation,
- do not modify workspace unless the user explicitly asks.
